def get_total_payment_request(doctype, name):

    #reference_name = doctype.name

    #paymentrequeststotal = frappe.db.sql("""
    #    SELECT SUM
    #        (grand_total) as Grand_Total
    #    FROM `tabPayment Request`
    #    WHERE docstatus = 2 AND reference_name = %(reference_name)s
    #    ORDER BY posting_date, referenceName""", {'referenceName': reference_name}, as_dict=1)


    
    #balanceTotal = 0

    #for prquest in paymentrequests:
    #    grandTotal = prquest['grand_total']
    #    balanceTotal = grandTotal + balanceTotal
    
    #return [paymentrequeststotal]

    pass